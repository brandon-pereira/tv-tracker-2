import React from 'react';

import { Container } from './ShowsContainer.styles';

function ShowsContainer({ children }) {
    return <Container>{children}</Container>
}

export default ShowsContainer;